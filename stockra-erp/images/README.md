# Screenshots — drop them here

The showcase `../README.md` references the image filenames below. Save your captures into **this folder** with these exact names and they'll appear automatically (GitHub serves them via `?raw=true` links).

PNG recommended. Landscape (roughly 16:10) looks best in the 3-column grid. Crop out anything client-identifying (customer names, real order data) before saving — this repo is public.

| Filename | What it should show |
|---|---|
| `dashboard.png` | Main dashboard with KPIs / analytics |
| `sales-order-detail.png` | A sales order detail page (fulfillment, totals, margin) — your strongest single screen |
| `inventory-stock.png` | Stock / inventory list across shops or warehouses |
| `multi-currency.png` | Exchange rates or a multi-currency view |
| `purchase-order.png` | A purchase order (ideally showing the approval workflow) |
| `invoice-jp.png` | A Japanese invoice |
| `goods-receipt.png` | A goods receipt / receiving screen |
| `admin-console.png` | The platform-admin console (organizations, plans, billing) |
| `analytics.png` | A sales or finance analytics view |

Optional extras (not referenced yet — add and I'll wire them in if you want):
`hero.png` (a banner for the top of the README), `logo.png` (from your Stockra brand assets).

Fewer is fine — the grid still reads well with 6. Any image you skip just shows a broken tile until added, so either supply all nine or tell me which to remove from the README.
